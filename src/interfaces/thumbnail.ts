export default interface Thumbnail {
    template:string,
    name:string,
    text: string,
    image?: string,
    title: string,
    channelName?: string,
    numViews?: string,
    date?: string,
    avatar?: string,
    status?: string
}